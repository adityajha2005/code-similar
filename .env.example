# Optional: GitHub Personal Access Token for API scans (higher rate limits + private repos).
# Create at https://github.com/settings/tokens — "repo" scope if you scan private repositories.
# Copy to .env.local (never commit .env.local).
GITHUB_TOKEN=
